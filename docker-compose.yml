services:
  research-agent-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - MONGODB_URI=mongodb://mongo:example@mongodb:27017/?authSource=admin
      - MONGODB_DB=appdb
      - OPENAI_API_KEY=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
      - PUBMED_API_KEY=0521afc7bb7c9e3baffcb3e5f21f7c4ca00a
      - PUBMED_EMAIL=villasica.serge@gmail.com
    # Uncomment to enable live reload in development:
    volumes:
     - .:/app
     - /app/__pycache__
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      mongodb:
        condition: service_healthy
    restart: unless-stopped

  mongodb:
    image: mongo:7.0
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=mongo
      - MONGO_INITDB_ROOT_PASSWORD=example
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--username", "mongo", "--password", "example", "--eval", "db.runCommand({ ping: 1 })"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mongo-data: 